const fs = require('fs');
const path = require('path');
const file = path.join(__dirname, 'src', 'Pages', 'CreateMonitor', 'index.tsx');
let text = fs.readFileSync(file, 'utf8');
const brokenPattern = '                    )\r\n                            />\r\n                            {watchedUseAdvancedMatching && (';
const brokenPatternLF = brokenPattern.replace(/\r\n/g, '\n');
let found = false;
if (text.includes(brokenPattern)) {
  found = true;
  text = text.replace(brokenPattern, '                }\r\n            />\r\n\r\n            {(watchedType === "http" ||\r\n                watchedType === "grpc" ||\r\n                watchedType === "websocket") && (\r\n                <ConfigBox\r\n                    title={t("pages.createMonitor.form.ignoreTls.title")}\r\n                    subtitle={t("pages.createMonitor.form.ignoreTls.description")}\r\n                    rightContent={\r\n                        <Controller\r\n                            name="ignoreTlsErrors"\r\n                            control={control}\r\n                            render={({ field }) => (\r\n                                <Stack\r\n                                    direction="row"\r\n                                    alignItems="center"\r\n                                    spacing={theme.spacing(SPACING.LG)}\r\n                                >\r\n                                    <Switch\r\n                                        checked={field.value ?? false}\r\n                                        onChange={(e) => field.onChange(e.target.checked)}\r\n                                    />\r\n                                    <Typography>\r\n                                        {t("pages.createMonitor.form.ignoreTls.option.tls.label")}\r\n                                    </Typography>\r\n                                </Stack>\r\n                            )}\r\n                        />\r\n                    }\r\n                />\r\n            )}\r\n\r\n            {watchedUseAdvancedMatching && (\r\n');
} else if (text.includes(brokenPatternLF)) {
  found = true;
  text = text.replace(brokenPatternLF, '                }\n            />\n\n            {(watchedType === "http" ||\n                watchedType === "grpc" ||\n                watchedType === "websocket") && (\n                <ConfigBox\n                    title={t("pages.createMonitor.form.ignoreTls.title")}\n                    subtitle={t("pages.createMonitor.form.ignoreTls.description")}\n                    rightContent={\n                        <Controller\n                            name="ignoreTlsErrors"\n                            control={control}\n                            render={({ field }) => (\n                                <Stack\n                                    direction="row"\n                                    alignItems="center"\n                                    spacing={theme.spacing(SPACING.LG)}\n                                >\n                                    <Switch\n                                        checked={field.value ?? false}\n                                        onChange={(e) => field.onChange(e.target.checked)}\n                                    />\n                                    <Typography>\n                                        {t("pages.createMonitor.form.ignoreTls.option.tls.label")}\n                                    </Typography>\n                                </Stack>\n                            )}\n                        />\n                    }\n                />\n            )}\n\n            {watchedUseAdvancedMatching && (\n');
}
fs.writeFileSync(path.join(__dirname, 'client-repair-result.txt'), found ? 'FOUND' : 'NOT_FOUND', 'utf8');
if (found) fs.writeFileSync(file, text, 'utf8');
